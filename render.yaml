services:
  - type: web
    name: interview-prep
    env: node
    region: singapore
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3456
      - key: NO_GATEWAY
        value: '1'
